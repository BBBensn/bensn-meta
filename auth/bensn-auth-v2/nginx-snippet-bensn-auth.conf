# /etc/nginx/snippets/bensn-auth.conf
# Einbinden mit:  include snippets/bensn-auth.conf;
# in jedem geschützten location-Block

# Auth-Request → bensn-auth Service
auth_request /auth/verify;

# Bei 401: weiterleiten zur Login-Seite mit ?next= für Rückkehr
error_page 401 = @bensn_login_redirect;
